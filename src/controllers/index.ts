import { createCategoryController, listCategoryPropertiesController } from "./categories.controller";
import { loginController } from "./login.controller";
import { createRealStateController, listRealStatesController } from "./realState.controller";
import { createScheduleController, listPropertiesSchedulesController } from "./schedules.controller";
import { createUserController, listUsersController, updateUserController, softDeleteUserController } from "./user.controller";

export {
    createCategoryController,
    listCategoryPropertiesController,
    loginController,
    createRealStateController,
    listRealStatesController,
    createScheduleController,
    listPropertiesSchedulesController,
    createUserController,
    listUsersController,
    updateUserController,
    softDeleteUserController
}